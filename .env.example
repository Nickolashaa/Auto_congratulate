# Telegram API credentials (get from https://my.telegram.org/apps)
API_ID=your_api_id
API_HASH=your_api_hash

# Session name (any name for your session file)
SESSION_NAME=my_session

# Delay between messages in seconds (to avoid flood limits)
MESSAGE_DELAY=1.5
